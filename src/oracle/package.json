{
  "name": "@marcelo-ochoa/server-oracle",
  "version": "0.7.1",
  "description": "MCP server for interacting with Oracle databases",
  "keywords": ["read-only-mcp", "oracle-database", "ai-agent", "llm-tool", "oci", "rag"],
  "license": "MIT",
  "author": "Marcelo Fabian Ochoa",
  "homepage": "https://modelcontextprotocol.io",
  "bugs": "https://github.com/marcelo-ochoa/servers/issues",
  "type": "module",
  "bin": {
    "mcp-server-oracle": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1",
    "oracledb": "^6.1.0"
  },
  "devDependencies": {
    "@types/oracledb": "^6.1.0",
    "shx": "^0.3.4",
    "typescript": "^5.0.0"
  }
}